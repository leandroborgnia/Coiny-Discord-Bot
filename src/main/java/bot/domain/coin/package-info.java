/**
 * Coin economy domain core — pure Java, no framework imports. Holds the coin value object, the
 * append-only double-entry posting model, the ledger policy, the outbound ports, and the typed
 * domain exceptions. The application layer depends inward on these types.
 */
package bot.domain.coin;
